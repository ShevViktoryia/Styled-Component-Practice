import { styled } from "styled-components";

const CardHeader = styled.h2`
  font-weight: 700;
  font-size: 16px;
  color: #000;
  margin: 20px 0;
  padding-left: 10px;
`;

export default CardHeader;
